import { mount } from '@vue/test-utils'
import { describe,expect,test } from 'vitest'

describe("La integracion del componente pokemonBOx" , () =>{
    // Test Integracion
    test("Que envie correctamente el imagen, name y number", async ()=>{

    });
    // Test Integracion
    test("El url que recibe la prop 'to' es correcta ", async ()=>{

    });
       // Test Unitario
    test("Renderiza correctamente la imagen (img)", () => {});

    // Test Unitario
    test("Renderiza correctamente el texto name y number", () => {});
});